info:
  name: refresh
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/auth/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
